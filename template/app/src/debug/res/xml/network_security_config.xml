<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug builds only. User-installed certificates are trusted so a proxy such as Charles or
  Proxyman can show the traffic, and plain HTTP is allowed to a backend running on this machine,
  which the emulator reaches as 10.0.2.2. Nothing here reaches a release build.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
